import React, { Component } from 'react'

export default class coperationbank extends Component {
    render() {
        return (
            <div>
                <h1> Coperation is located at @ {this.props.location }</h1>
            </div>
        )
    }
}
